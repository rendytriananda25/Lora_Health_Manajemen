name: lora_1
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  audioplayers: ^5.2.1
  cupertino_icons: ^1.0.8
  mobile_scanner: ^7.1.4
  http: ^1.1.0
  firebase_core: ^4.4.0
  firebase_auth: ^6.1.4
  firebase_database: ^12.1.2
  geolocator: ^10.1.0
  geolocator_android: ^4.4.0
  flutter_map: ^6.1.0
  latlong2: ^0.9.0
  google_sign_in: ^6.2.2
  intl: ^0.19.0
  flutter_facebook_auth: ^7.0.0
  
  # === TAMBAHAN FUNGSI ALARM & SOUND ===
  alarm: ^4.1.1
  file_picker: ^8.1.2
  path_provider: ^2.1.2
  permission_handler: ^12.0.1
  smooth_page_indicator: ^2.0.1
  cloud_firestore: ^6.1.2
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.13.1

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/audio/alarm.mp3

flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/images/new.png"
  image_ratio_android: 0.8